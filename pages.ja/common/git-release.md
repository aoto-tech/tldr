# git release

> リリース用のGitタグを作成する。
> `git-extras` の一部。
> 詳細情報: <https://manned.org/git-release>。

- リリースを作成してプッシュする:

`git release {{タグ名}}`

- 署名付きリリースを作成してプッシュする:

`git release {{タグ名}} -s`

- メッセージ付きリリースを作成してプッシュする:

`git release {{タグ名}} -m "{{メッセージ}}"`
