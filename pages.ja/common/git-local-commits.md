# git local-commits

> リモートのoriginへプッシュされていないローカルコミットを表示する。追加の引数はすべて `git log` に直接渡される。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-local-commits>。

- プッシュされていないコミットを表示する:

`git local-commits`
