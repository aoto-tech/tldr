# git ignore-io

> 定義済みのテンプレートから `.gitignore` ファイルを生成する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-ignore-io>。

- 利用可能なテンプレートを一覧表示する:

`git ignore-io list`

- `.gitignore` テンプレートを生成する:

`git ignore-io {{項目A,項目B,...}}`
