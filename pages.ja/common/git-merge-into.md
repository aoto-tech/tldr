# git merge-into

> あるブランチを別のブランチへマージする。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-merge-into>。

- 元のブランチを指定した対象ブランチへマージする:

`git merge-into {{元のブランチ}} {{対象ブランチ}}`

- 現在のブランチを指定した対象ブランチへマージする:

`git merge-into {{対象ブランチ}}`
