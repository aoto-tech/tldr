# git cp

> 履歴を保持したまま、既存のファイルを新しい場所へコピーする。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-cp>。

- Gitリポジトリ内の既存ファイルを同じディレクトリ内にコピーする:

`git cp {{ファイル}} {{新しいファイル}}`

- Gitリポジトリ内の既存ファイルを別の場所へコピーする:

`git cp {{ディレクトリ/サブディレクトリ/ファイル}} {{ディレクトリ/サブディレクトリ/新しいファイル}}`
