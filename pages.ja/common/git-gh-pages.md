# git gh-pages

> 現在のリポジトリ内に `gh-pages` という名前の新しいブランチを作成する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-gh-pages>。

- 現在のディレクトリにあるリポジトリ内にGitHub Pages用ブランチを作成する:

`git gh-pages`
