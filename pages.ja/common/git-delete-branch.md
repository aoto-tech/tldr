# git delete-branch

> ローカルとリモートのGitブランチを削除する。
> `git-extras` の一部。チェックアウト中のブランチを削除する場合、リモートブランチだけが削除される。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-delete-branch>。

- ローカルとリモートのGitブランチを1つ以上削除する:

`git delete-branch {{ブランチ名1 ブランチ名2 ...}}`
