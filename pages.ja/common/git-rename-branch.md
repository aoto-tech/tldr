# git rename-branch

> Gitブランチの名前を変更する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-rename-branch>。

- 現在のブランチの名前を変更する:

`git rename-branch {{新しいブランチ名}}`

- 指定したブランチの名前を変更する:

`git rename-branch {{古いブランチ名}} {{新しいブランチ名}}`
