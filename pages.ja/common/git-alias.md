# git alias

> Gitコマンドのショートカットを作成する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-alias>。

- すべてのエイリアスを一覧表示する:

`git alias`

- 新しいエイリアスを作成する:

`git alias "{{名前}}" "{{コマンド}}"`

- 既存のエイリアスを検索する:

`git alias ^{{名前}}`
