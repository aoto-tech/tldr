# npm explore

> インストールされている npm パッケージを参照します。
> 詳細情報: <https://docs.npmjs.com/cli/npm-explore/>。

- インストールされているパッケージを参照します:

`npm explore {{package_name}}`

- パッケージを参照し、その中でコマンドを実行します:

`npm explore {{package_name}} -- {{command}}`
