# git show-merged-branches

> 現在のHEADにマージ済みのすべてのブランチを出力する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-show-merged-branches>。

- 現在のHEADにマージ済みのすべてのブランチを出力する:

`git show-merged-branches`
