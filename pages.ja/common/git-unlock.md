# git unlock

> Gitリポジトリ内のファイルのロックを解除し、コミットによる変更を可能にする。
> `git-extras` の一部。
> 参照: `git lock`。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-unlock>。

- ロックされていたローカルファイルへの変更をコミットできるようにする:

`git unlock {{ディレクトリ/サブディレクトリ/ファイル}}`
