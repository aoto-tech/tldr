# npm edit

> 現在のプロジェクトで依存関係を選択し、デフォルトのエディター (`$EDITOR`) でパッケージ フォルダーを開きます。
> 編集後、パッケージは再構築され、コンパイルされたパッケージの変更が反映されます。
> 詳細情報: <https://docs.npmjs.com/cli/npm-edit/>。

- ローカルにインストールされたパッケージをデフォルトのエディターで編集します:

`npm edit {{package_name}}`
