# git info

> Gitリポジトリの情報を表示する。
> `git-extras` の一部。
> 詳細情報: <https://manned.org/git-info>。

- リモートの場所、リモートとローカルのブランチ、最新コミットのデータ、`.git/config` の設定を表示する:

`git info`

- リモートの場所、リモートとローカルのブランチ、最新コミットのデータを表示する:

`git info --no-config`
