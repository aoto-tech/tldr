# git clear

> `.gitignore` 内のファイルも含め、現在のブランチを新しくクローンした直後のようにGit作業ディレクトリを消去する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-clear>。

- すべての追跡済みファイルをリセットし、`.gitignore` に含まれるものも含めて追跡されていないファイルをすべて削除する:

`git clear`
