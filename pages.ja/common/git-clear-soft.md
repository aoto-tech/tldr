# git clear-soft

> `.gitignore` 内のファイルを除き、現在のブランチを新しくクローンした直後のようにGit作業ディレクトリを消去する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-clear-soft>。

- すべての追跡済みファイルをリセットし、追跡されていないファイルをすべて削除する:

`git clear-soft`
