# git setup

> ディレクトリにGitリポジトリを作成し、すべてのファイルをコミットする。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-setup>。

- 現在のディレクトリにGitリポジトリを作成し、すべてのファイルをコミットする:

`git setup`

- 指定したディレクトリにGitリポジトリを作成し、すべてのファイルをコミットする:

`git setup {{ディレクトリ/サブディレクトリ}}`
