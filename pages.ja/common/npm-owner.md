# npm owner

> 公開されたパッケージの所有権を管理します。
> 詳細情報: <https://docs.npmjs.com/cli/npm-owner/>。

- 新しいユーザーをパッケージのメンテナとして追加します:

`npm owner add {{username}} {{package_name}}`

- パッケージの所有者リストからユーザーを削除します:

`npm owner rm {{username}} {{package_name}}`

- パッケージのすべての所有者をリストします:

`npm owner ls {{package_name}}`
