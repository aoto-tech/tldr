# npm init

> `package.json` ファイルを作成します。
> 詳細情報: <https://docs.npmjs.com/cli/npm-init/>。

- プロンプトを使用して新しいパッケージを初期化します:

`npm init`

- 新しいパッケージをデフォルト値で初期化します:

`npm init {{[-y|--yes]}}`

- 特定の初期化子を使用して新しいパッケージを初期化します:

`npm init {{create-react-app}} {{my-app}}`
