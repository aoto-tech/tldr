# git delete-merged-branches

> `git branch --merged` に一覧表示されるブランチを、masterを除いて削除する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-delete-merged-branches>。

- マージ済みのブランチを削除する:

`git delete-merged-branches`
