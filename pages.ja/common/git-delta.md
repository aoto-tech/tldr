# git delta

> 別のブランチと異なるファイルを一覧表示する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-delta>。

- 現在チェックアウトしているブランチで `main` ブランチと異なるファイルを一覧表示する:

`git delta {{main}}`

- 指定した2つのブランチ間で異なるファイルを一覧表示する:

`git delta {{ブランチ1}} {{ブランチ2}}`
