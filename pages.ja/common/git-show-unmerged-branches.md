# git show-unmerged-branches

> 現在の `HEAD` にマージされていないすべてのブランチを出力する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-show-unmerged-branches>。

- 現在の `HEAD` にマージされていないすべてのブランチを出力する:

`git show-unmerged-branches`
