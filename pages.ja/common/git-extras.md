# git extras

> Gitの拡張コマンド集。
> 詳細情報: <https://manned.org/git-extras>。

- `git-extras` コマンドをインストールまたはアップグレードする:

`git extras update`

- ヘルプを表示する:

`git extras {{[-h|--help]}}`

- バージョンを表示する:

`git extras {{[-v|--version]}}`
