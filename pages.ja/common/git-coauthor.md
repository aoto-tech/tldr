# git coauthor

> 最新のコミットに別の作成者を追加する。このコマンドはGit履歴を書き換えるため、次回のプッシュでは `--force` が必要になる。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-coauthor>。

- 直前のGitコミットに作成者を追加する:

`git coauthor {{名前}} {{name@example.com}}`
