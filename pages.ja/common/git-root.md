# git root

> 現在のGitリポジトリのルートディレクトリを出力する。
> `git-extras` の一部。
> 詳細情報: <https://github.com/tj/git-extras/blob/main/Commands.md#git-root>。

- 現在のGitリポジトリの絶対パスを出力する:

`git root`

- 現在のGitリポジトリのルートから見た、現在の作業ディレクトリの相対パスを出力する:

`git root {{[-r|--relative]}}`
