# npm root

> `node_modules` ディレクトリへのパスを表示します。
> 詳細情報: <https://docs.npmjs.com/cli/npm-root/>。

- ローカルの `node_modules` ディレクトリへのパスを表示します:

`npm root`

- グローバル `node_modules` ディレクトリへのパスを表示します:

`npm root {{[-g|--global]}}`
