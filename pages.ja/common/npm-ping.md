# npm ping

> `npm` レジストリに ping を実行します。
> 詳細情報: <https://docs.npmjs.com/cli/npm-ping/>。

- デフォルトの `npm` レジストリに ping を実行します:

`npm ping`

- カスタム `npm` レジストリに ping を実行します:

`npm ping --registry {{custom_registry_url}}`
