# npm-name

> パッケージ名または組織名が npm で利用可能かどうかを確認します。
> 詳細情報: <https://github.com/sindresorhus/npm-name-cli>。

- 特定のパッケージ名が `npm` レジストリで使用できるかどうかを確認します:

`npm-name {{package}}`

- `npm` レジストリで類似のパッケージ名を検索します:

`npm-name --similar {{package}}`
