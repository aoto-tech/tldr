# systemctl rescue

> レスキューモードに入ります。
> `systemctl emergency` も参照してください。
> 詳細情報: <https://www.freedesktop.org/software/systemd/man/latest/systemctl.html#rescue>。

- レスキューモードに入ります:

`systemctl rescue`

- レスキュー モードを非同期的に開始します:

`systemctl rescue --no-block`
