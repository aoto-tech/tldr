# systemctl default

> システムのデフォルトモードに入ります。
> 詳細情報: <https://www.freedesktop.org/software/systemd/man/latest/systemctl.html#default>。

- ブロック操作としてデフォルト モードに入ります:

`systemctl default`

- 非同期操作としてデフォルト モードに入ります:

`systemctl default --no-block`
