# systemctl daemon-reload

> systemd マネージャー構成をリロードします。
> ユニットファイルを作成、変更、削除した後に使用してください。
> `systemctl reload` も参照してください。
> 詳細情報: <https://www.freedesktop.org/software/systemd/man/latest/systemctl.html#daemon-reload>。

- systemd をリロードして、ユニット ファイルの変更を適用します:

`systemctl daemon-reload`
