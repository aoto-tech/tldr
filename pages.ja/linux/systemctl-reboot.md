# systemctl reboot

> システムを再起動します。
> 詳細情報: <https://www.freedesktop.org/software/systemd/man/latest/systemctl.html#reboot>。

- システムを再起動します:

`systemctl reboot`

- 再起動して BIOS/UEFI メニューを表示します:

`systemctl reboot --firmware-setup`
