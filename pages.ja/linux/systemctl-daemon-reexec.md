# systemctl daemon-reexec

> 現在のユニットの状態を保持しながら、systemd を再実行します。
> `systemctl daemon-reload` も参照してください。
> 詳細情報: <https://www.freedesktop.org/software/systemd/man/latest/systemctl.html#daemon-reexec>。

- systemd を再実行します:

`systemctl daemon-reexec`
